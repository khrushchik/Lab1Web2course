@model IEnumerable<LABA1.Contracts>

@{
    ViewData["Title"] = "Контракты";
}

<h1>Контракты</h1>

@*<p>
        @if (ViewContext.HttpContext.User.IsInRole("admin"))
        {
            <form asp-action="Create">
                <input type="submit" value="Добавить" />
            </form>
        }
    </p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.RenterSort">
                    Арендатор
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.CarSort">
                    Автомобиль
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.StartDateSort">
                    Дата начала
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.DayNumberSort">
                    Количество дней
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.DayPriceSort">
                    Цена за сутки
                </a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (Contracts u in Model)
        {
            <tr>
                <td>@u.Renter.Name</td>
                <td>@u.Car.GovNumber</td>
                <td>@u.StartDate</td>
                <td>@u.DayNumber</td>
                <td>@u.DayPrice</td>
                <td>
                    @if (ViewContext.HttpContext.User.IsInRole("admin"))
                    {
                        <a asp-action="Edit" asp-route-id="@u.Id">Редактировать</a>
                        <a asp-action="Delete" asp-route-id="@u.Id">Удалить</a>
                    }
                </td>

            </tr>
        }
    </tbody>
    @*<thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.StartDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DayNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DayPrice)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Renter)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Car)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.StartDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DayNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DayPrice)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Renter.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Car.GovNumber)
                    </td>
                    <td>
                        @if (ViewContext.HttpContext.User.IsInRole("admin"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.Id">Редактировать</a>
                            <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
                        }
                    </td>
                </tr>
        }
            </tbody>*@
</table>
<img style="position: absolute; bottom:30px;right:15px; " src="~/css/fon.png" border="0" width=400px; />
